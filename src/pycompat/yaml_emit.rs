//! Subset of PyYAML's `Emitter` for `safe_dump(default_flow_style=False)` (block style, sorted keys, indentless sequences inside mappings, `[]`/`{}` for empty containers, scalar style selection plain/single-quoted/double-quoted using the implicit resolvers, line folding at width 80, `true`/`false`/`null`, ints plain). Used by the yaml formatter and `bandit-config-generator`.
//!
//! Status: stub (see PLAN.md).
