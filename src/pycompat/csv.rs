//! `csv.writer` excel dialect: fields joined by `,`, quoted with `"` (doubled inside) only when containing the delimiter, a quote, `\r` or `\n`; rows terminated by `\r\n`.
//!
//! Status: stub (see PLAN.md).
