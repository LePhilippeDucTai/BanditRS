//! Python `string.Formatter().parse` + the format-spec mini language for `int` and `str` values (`{line:03}`, `{test_id:^8}`, `{relpath:20.20s}`, `{msg:>20}`), with Python's `ValueError` messages (`Single '}' encountered in format string`, `Single '{' encountered in format string`, `expected '}' before end of string`, `'=' alignment not allowed in string format specifier`). Used by the custom formatter.
//!
//! Status: stub (see PLAN.md).
