//! Ports of `tests/unit/formatters/*.py` (docs/spec/cli_formatters_tests.md §C.7):
//! a `Manager` populated with one issue (`fname = <tmpfile>`, lineno 4,
//! linerange [4], test `hardcoded_bind_all_interfaces`, text
//! `Possible binding to all interfaces.`, col 8, end col 16) is rendered by
//! each formatter and the output parsed back.
//! TODO(M6).

#[test]
#[ignore = "formatters not implemented yet (PLAN.md M6)"]
fn placeholder_formatter_tests() {
    unimplemented!("see docs/spec/cli_formatters_tests.md §C.7");
}
